{
  "installCommand": "npm install && npm --prefix web install",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/automation/dispatch",
      "schedule": "0 5 * * *"
    }
  ]
}
